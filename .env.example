NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Use "true" to force local mock data while the backend is not running.
# When NEXT_PUBLIC_API_BASE_URL is empty, mocks are used automatically.
NEXT_PUBLIC_USE_API_MOCKS=false
